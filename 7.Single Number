class Solution {
    public int singleNumber(int[] nums) {
        int l = nums.length;
        int r=nums[0];
        for(int i=1;i<l;i++){
            r^=nums[i];
            }

        return r;
    }
}
