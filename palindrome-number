class Solution {
    public boolean isPalindrome(int x) {
        String s=String.valueOf(x);
        int r=s.length();
        for(int i=0;i<r/2;i++){
            if(s.charAt(i)!=s.charAt(r-i-1))
                return false;
        }
        return true;
    }
}
